# 09 数据模型与任务编排

> 以半成品现有模型为基础，列出全系统表清单、队列与调度设计。

## 一、表清单总览

### 现有（复用）
| 表 | 作用 |
|---|---|
| `products` | 商品主表：来源/匹配/询价/生图/定价/上架状态全字段 |
| `product_library` | 商品库：归一化名称/指纹去重/历史表现 |
| `product_fingerprint_claims` | 指纹认领（防并发重复入库） |
| `source_cursors` / `source_board_states` | 选品账本：游标/节流/熔断/断点 |
| `source_platform_states` | 平台级风控状态（risk_control/探针恢复） |
| `source_collection_events` / `source_runs` | 采集事件/批次记录 |
| `product_source_evidence` | 来源证据（平台+榜单+item） |
| `image_batches` / `image_assets` | 生图批次/图片资产（审核状态/拒绝原因/审计） |
| `category_listing_memory` | 类目上架记忆（通过/拒审经验） |
| `suppliers` / `sku` | 供应商/逐 SKU 成本 |
| `pricing` / `upload_history` / `wechat_upload_logs` | 定价/上架历史/微信操作日志 |
| `workflow_jobs` / `tasks` / `logs` | 任务队列/任务/日志 |
| `ai_generation_logs` | AI 调用留痕 |

### 新增（本轮方案）
| 表 | 作用 |
|---|---|
| `assets` | 素材库：视频/图片，双去重，评估标签，平台素材 ID（见 05） |
| `ad_campaigns` | 托管投放计划：目标类型/ROI/素材绑定/诊断/状态（见 08） |
| `ad_runs` | 托管执行记录（租约/错误分类，复用 WorkflowJob） |
| `ad_report_snapshots` | 投放报表快照：曝光/花费/成交/补贴/诊断 |
| `ad_account_states` | 投放账户：余额/风控/暂停 |
| `app_config`（扩展） | 类目白名单/权重/预算上限等配置项配置化 |

## 二、任务队列设计（`workflow_jobs` + `workflow_runner`）

### 阶段（stage）
```
source_collect（选品采集，调度器驱动）
alibaba_quote（1688 逐 SKU 询价）→ taobao_reference（淘宝素材）→ image_generation（生图）
listing_upload（上架，OpenAPI 主 / UI 兜底）
shop_ads_run（托管投放执行）
shop_ads_report（投放报表回读）
```

### 状态与错误分类码表
| error_code | 含义 | 重试 |
|---|---|---|
| `VERIFICATION_REQUIRED` | 验证码/安全验证 | 不自动重试 → 人工接管，单任务暂停 |
| `AUTH_REQUIRED` | 登录失效 | 不自动重试 → 人工登录后从断点续跑 |
| `RATE_LIMIT` | 限流/频繁 | 180s 退避 |
| `TIMEOUT` | 超时 | 60s 退避 |
| `NO_MATCH` / `INSUFFICIENT_REFERENCES` | 无同款/素材不足 | 120s 退避 |
| `PLATFORM_REJECT` | 平台驳回（资质/内容） | 记录原因，转人工/修复候选 |
| `UNEXPECTED` | 未知 | 60s 退避，留证据 |

### 关键机制
- **租约**：`lease_owner` + `lease_expires_at`（45min 过期回收），进程重启 `recover_after_process_restart`
- **失败隔离**：单商品 blocked 不影响其他商品排队
- **后继阶段**：taobao_reference 完成才 enqueue image_generation；上架完成才可入托管队列
- **幂等**：`(product_id, stage, generation_version)` 唯一约束防重复入队

## 三、调度器设计（继承 `SourcingScheduler` 模式，进程化）

- **账本**：每（平台,榜单）游标/`next_run_at`/`completed_for_date`/证据 JSON
- **节流**：throttle 0~4 级，间隔 ×1/2/4/8/16；失败次数逐级提升
- **熔断**：连续失败 ≥2 → `risk_control` 暂停平台，探针板恢复
- **人工接管**：`waiting_login`（暂停30min）/ `waiting_verification`（暂停60min），用户完成后面向断点续跑
- **实时榜降频**：连续空转 24 次，小时轮询 → 日轮询
- **进程化建议**：调度器从 uvicorn daemon 线程迁出为独立进程（或 systemd 服务），重启自愈

## 四、前端状态机（复用 `workflow.ts`，扩展）

```
1 已选品 → 2 淘宝素材 → 3 询价(1688) → 4 生图 → 5 图片审核
         → 6 待上架/已上架 → 7 托管投放(待托管/托管中/暂停/结束)
```
- 「可上架品」点击后必须返回 request_id/阶段/错误，不静默消失
- 托管看板对齐后台列表列：商品/目标出价/诊断/曝光/花费/成交/补贴/操作