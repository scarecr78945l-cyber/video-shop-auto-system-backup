# 01 开源项目盘点与借鉴

> 注：本文基于对 GitHub 开源生态的既有掌握盘点。「实锤」为可确认存在的知名项目；其余标记「需验证」，以仓库 README 为准。Web 搜索工具额度受限时，用文末关键词自行检索。

## 一、按五大功能分类

### 1. 自动上架 / 小店 API 对接（系统地基）

**① 微信小店官方开放接口（Channels Open API）** — 必须用
- 位置：`developers.weixin.qq.com/doc/channels/API/`（微信小店板块，接口域 `channels`）
- 能力：SPU/SKU 创建编辑、类目资质、库存、价格、订单、售后、物流、数据看板
- 优：官方、全量、稳定，是「自动上架/改价/库存同步」的地基
- 劣：需企业主体、类目资质审核、接口调用配额（需排队+幂等重试）；**选品/带货数据无 API**

**② 社区语言封装（Go/Python/Node）** — 谨慎使用
- 关键词：`wechat channels shop api`、`微信小店 openapi` 能找到若干封装
- 优：省去 SHA256 签名+时间戳窗口
- 劣：多个人维护、微信接口变更后更新慢
- 结论：**不直接依赖**，按官方文档自写薄封装层（签名/重试/限额队列约几百行）

### 2. 自动收集素材

**① `JoeanAmier/TikTokDownloader`** ★实锤、高价值
- 抖音/TikTok 批量下载，近期版本已覆盖快手、B站、小红书、微博、**视频号**（以 README 为准）
- 优：活跃维护、社区大、支持关键词/达人主页/合集、签名处理成熟、可作库调用
- 劣：核心优化面向抖音，视频号模块相对后加且稳定性弱；只产出视频文件，无带货数据

**② `Evil0ctal/Douyin_TikTok_Download_API`** ★实锤、高价值
- 自部署下载 HTTP API，适合作为系统内部「素材下载中台」
- 优：接口化调用、自带任务队列、可多开提速
- 劣：主攻抖音生态，视频号支持基本没有

**③ 视频号专用下载器（零散小项目）**
- 关键词：`视频号 下载 wechat video downloader`
- 优：针对性
- 劣：个人维护、改版即失效；**建议只用上述项目视频号功能 + 自研一层**

### 3. 自动选品（空缺地带，需自建）

- 现状：GitHub 几乎没有完整「视频号选品」开源项目。数据侧不开放（销量/带货榜无官方 API），成熟能力全在商业 SaaS（蝉妈妈、飞瓜、选品猫等）
- 可参考零散资产：抖音电商数据采集脚本（登录态保活、签名、限速范式）；`RSSHub` ★实锤（订阅路由做监控触发器）
- 结论：选品 = 自建流水线（素材热度采集 + 商品库打标 + 规则/LLM 打分）

### 4. 自动小店投放

- **小店投放（商品托管/随心推）目前无官方开放 API**，只能后台页面操作
- 投放动作自动化唯一现实路径：浏览器 UI 自动化（Playwright 复用共享 Chrome）
- 腾讯广告 Marketing API（`developers.e.qq.com`）是另一条投放线（信息流/广告主），**本项目投放口径不采用**

### 5. 自动素材优化

- 无成品系统，组件组合即可：
  - 文案/标题：大模型 API（DeepSeek/GLM）+ 结构化输出
  - 批量剪辑：`ffmpeg`（裁剪/拼接/转码/字幕/多比例出片）+ `moviepy`（Python 封装）
  - 封面/AIGC 图：Pillow 文字排版 / 出图模型（Kimi 规划 + Wan 生图，半成品项目已验证）

## 二、优劣小结

| 功能 | 最佳开源资产 | 优 | 劣 | 结论 |
|---|---|---|---|---|
| 上架 | 微信小店官方 API | 稳定、全量 | 资质+限流 | 自写薄封装 |
| 素材收集 | TikTokDownloader + 下载 API 中台 | 批量、可集成 | 视频号支持弱 | 复用+补视频号层 |
| 选品 | 无成品 | — | 数据不开放 | 自建 AI 打分 |
| 投放 | 无（后台 UI 操作） | 无转化不扣费 | 无 API、纯 UI | Playwright UI 自动化 |
| 素材优化 | ffmpeg + LLM 组合 | 成本低 | 需调参 | 自建流水线 |

## 三、检索词备用（额度恢复后使用）

- `微信小店 openapi github` / `channels shop api`
- `视频号 下载 github` / `wechat video downloader`
- `视频号小店 铺货 自动上架`
- `小店随心推 自动化`