# 02 半成品项目评估

> 对象：`视频号自动上架系统_迁移包_20260811_155324`（FastAPI + SQLite + Playwright + Next.js，测试 534 passed）

## 一、项目画像

- **后端**：FastAPI（`app/main.py`）+ SQLAlchemy + SQLite + APScheduler/自研线程调度器
- **前端**：Next.js + React + Tailwind（商品队列、图片审核工作台、工作流阶段视图）
- **自动化**：Playwright 复用共享 Chrome（CDP 端口），全部平台走 UI 自动化，无官方 API
- **文档体系**：`docs/superpowers/plans|specs`（15 份设计与 11 份实施计划）、`chat/CHAT_HANDOFF.md`（业务规则沉淀）
- **数据**：`app.db` 快照（498 products / 521 product_library / 939 tasks / 657 image_assets / 40 workflow_jobs）

## 二、五功能成熟度对照

| 功能 | 现状 | 成熟度 | 说明 |
|---|---|---|---|
| 自动选品 | ✅ 完整 | ⭐⭐⭐⭐ | 考古加/有米云/视频号商机中心三源采集；SourceBoardState 账本+游标+节流+风控熔断；打分（趋势35/利润30/售后20/供给15）；合规三态过滤 |
| 自动上架 | ✅ 大量实现 | ⭐⭐⭐ | Playwright 驱动真实微信小店（181KB 脚本 open_wechat_upload_product.py）：AI 选类目、SKU 逐项填写、限购、草稿/提交/审核检测 |
| 自动投放 | ❌ 完全缺失 | ⭐ | 无任何投放模块 |
| 自动素材优化 | ✅ 有实现 | ⭐⭐⭐ | AI 主图生成（Kimi 规划 + Wan 生图）、5 张 1:1 主图、详情图、图片审核工作台、类目记忆 |
| 自动收集素材 | ✅ 有实现 | ⭐⭐⭐ | 淘宝参考图、1688 以图搜款、素材下载缓存、去重、素材库 |

## 三、值得直接借鉴的亮点

### 1. 调度与容错（`sourcing_scheduler.py`）— 最值得抄
- **来源账本**：每榜单记录游标/`last_item_id`/`completed_for_date`；静态榜扫完当天跳过，实时榜按「小时轮询 → 连续空转 24 次降日轮询」自动降频
- **风控熔断**：失败次数 → throttle 0~4 级指数退避（间隔 ×1/2/4/8/16）；连续失败 2 次进 `risk_control` 暂停整平台，用探针板恢复
- **断点续跑**：`resume_on_startup()` 恢复进程崩溃后的任务；租约 `CLAIM_LEASE_MINUTES=45` 过期自动回收

### 2. 任务队列与失败隔离（`workflow_runner.py`）
- `WorkflowJob` 持久化队列：stage 分阶段独立 worker（alibaba_quote → taobao_reference → image_generation）
- **错误分类**：`VERIFICATION_REQUIRED` / `AUTH_REQUIRED` / `RATE_LIMIT`(重试180s) / `TIMEOUT`(60s) / `NO_MATCH`(120s)
- **单商品阻塞不拖垮队列**：一个商品等验证码，其他商品继续排队——全自动系统的命脉

### 3. 业务规则沉淀（`CHAT_HANDOFF.md` + `pricing.py` + `compliance.py`）
- 标题 15–35 字符；至少 5 张 1:1 主图；逐 SKU 真实询价（订单确认页读价，不下单）；同款取最低有效总成本；拿到真实链接才算「已上架」
- 定价阶梯：成本≤3→9，≤5→19.9，≤10→29.9，≤15→49.9（`pricing.py`）
- 合规三态：`hard_reject` / `candidate` / `manual_review`；品牌侵权词、功效词门槛、供应链词过滤（`compliance.py`）

### 4. 工程意识
- 534 测试覆盖、证据 JSON 留痕（`evidence_json`）、敏感信息脱敏（`_redact_text`）、迁移包带 SHA-256 清单

## 四、问题与风险

### 致命级
1. **上架走 UI 自动化而非官方 API**：系统已出现 `page_changed`、选择器证据、`repeated_page` 检测，说明平台改版即崩的亏已吃过多次 → 主链路必须迁移官方 channels API
2. **投放完全缺失**：无 ROI 数据回流，选品打分四维（趋势/利润/售后/供给）没有「投放转化」维度，系统无法自我进化

### 高风险
3. **SQLite 单文件 + 多线程 worker**：328MB 库，3 worker 并发写，锁竞争随数据量恶化
4. **调度器是 daemon 线程**：挂在 uvicorn 进程内，重启即丢（虽有恢复逻辑，仍脆弱）
5. **便携 Chrome 硬编码路径**（`analytics_browser_session.py`）：换机器必炸，需环境变量化
6. 老版本依赖：pydantic 1.10 / FastAPI 0.103

### 中低风险
7. 超长脚本堆砌：`fetch_wechat_opportunities.py`(75KB)、`fetch_1688_quotes.py`(84KB)、`open_wechat_upload_product.py`(181KB) 不可维护
8. 选品面人为收窄：`ALLOWED_CATEGORIES` 仅放行家居/日用品等 9 类，其余卡人工
9. 无鉴权：API 全开放（CORS 仅限 localhost:3000）
10. 素材优化只覆盖图片，无**视频**素材链路——视频号带货硬伤

## 五、借鉴与改造清单

**直接拿来用（省 2–3 周）**
- 来源采集调度器（账本+节流+熔断）→ 原样搬
- WorkflowJob 队列 + 错误分类 + 失败隔离 → 原样搬
- 定价阶梯、合规过滤、选品打分 → 原样搬，扩大类目白名单为配置项
- 534 测试作为回归基线

**必须重构**
1. 上架双轨制：新建 `wechat_openapi` 适配器走官方 channels API；Playwright 脚本降级兜底
2. 补投放模块：小店投放（商品托管）自动化，ROI 回传作选品打分第 5 维度
3. 补视频素材链路：视频采集 + ffmpeg 9:16 二创 + 素材审核闸门
4. 存储与调度升级：SQLite → PostgreSQL；线程调度 → 独立进程
5. 数据回流闭环：上架 → 托管投放 → 订单成交 → 回写打分与素材评分