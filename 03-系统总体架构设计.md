# 03 系统总体架构设计

> 目标系统：**视频号微信小店全自动全流程系统**。五条链路：自动选品、自动上架、自动小店投放（商品托管）、自动素材优化、自动收集素材。

## 一、分层架构

```
┌─ 采集层 ──────────────────────────────┐
│ 素材采集（视频号/抖音/快手/小红书视频）   │→ 素材库（MinIO/OSS）+ ffmpeg 标准化
│ 选品采集（抖店电商罗盘/有米云/视频号商机中心）│→ 断点账本 + 节流熔断
│ 竞品/榜单热度采集（定时）                │→ PostgreSQL 热度特征表
└──────────────────────────────────────┘
┌─ AI 决策层 ───────────────────────────┐
│ 选品打分（规则权重+投放转化+LLM 复核）   │→ 待托管/待上架商品池（人工闸门可选）
│ 素材优化（LLM 文案 + 9:16 视频二创 + AI 主图）│→ 多版本素材（A/B 候选，硬性规格锁定）
└──────────────────────────────────────┘
┌─ 执行层 ──────────────────────────────┐
│ 上架引擎（微信小店 OpenAPI 主链路）      │→ SPU/SKU/库存/价格，幂等+限额队列
│   └ Playwright 兜底（官方未覆盖操作）    │
│ 投放引擎（小店投放·商品托管）            │→ 添加托管商品 → 设 ROI → 绑优选素材
│ 执行队列（WorkflowJob 失败隔离+人工接管） │→ 验证码/风控单任务暂停，不阻塞全队列
└──────────────────────────────────────┘
┌─ 调度与监控 ──────────────────────────┐
│ 调度器（账本/节流/熔断/断点恢复）        │→ 独立进程，进程重启自愈
│ 监控告警（余额/预算/止损/诊断回读）      │→ 三层预算硬约束 + 一键全停
│ 管理后台（商品/素材/托管/报表看板）      │
└──────────────────────────────────────┘
```

## 二、数据流闭环（系统自我进化）

```
采集 → 素材库 → 素材优化(9:16 视频/主图/文案)
  ↓                    ↓
选品打分(五维) → 商品池 → 上架(OpenAPI) → 销售中商品
                              ↓
                        小店投放·商品托管(无转化不扣费)
                              ↓
           托管报表回读（曝光/花费/成交/补贴/诊断）
                              ↓
   成交 ROI 回写：①选品打分加「投放转化」维度
                 ②素材评估标签（高效/潜力/探索期）回流素材优化
                 ③投放止损规则（换素材/调 ROI/暂停）→ 下一批更准
```

## 三、技术栈

| 层 | 选型 | 说明 |
|---|---|---|
| 后端 | Python 3.12 + FastAPI | 复用半成品基线 |
| 任务调度 | 独立进程调度器（继承现有 SourcingScheduler 模式，进程化） | 可后续换 Celery |
| 存储 | PostgreSQL（业务数据） / MinIO（素材） / Redis（队列缓存） | 从 SQLite 迁移 |
| 浏览器自动化 | Playwright + 共享 Chrome（CDP） | 仅用于无 API 的界面操作（投放、兜底上架） |
| AI | DeepSeek（文案/标题/复核）+ Kimi 规划 + Wan 生图 | 半成品已验证 |
| 视频处理 | ffmpeg / moviepy | 9:16 二创硬规格输出 |
| 前端 | Next.js + React + Tailwind（复用现有控制台） | 加托管看板/素材库页 |

## 四、关键架构决策

1. **上架双轨制**：官方 OpenAPI 为主，Playwright 兜底——平台 UI 改版不再致命
2. **投放=商品托管**：无转化不扣费、平台自动投流，自动化只需三个决策点（托管商品/目标 ROI/素材），烧钱风险最低
3. **素材规格锁定**：投放素材硬规格（9:16、≥720×1280、MOV/MP4、≤500M、5~300s）写进素材优化模块输出参数
4. **预算三层硬约束**：单笔预算 / 日总预算 / 计划总预算同时生效，后台一键全停
5. **人工闸门保留**：选品复核（可配置放松）、上架最终确认、投放素材预审——合规兜底
6. **全链路幂等 + 断点恢复**：所有写入幂等，进程重启从数据库状态恢复

## 五、模块边界（文件层映射）

| 模块 | 新增/复用 |
|---|---|
| 选品 | 复用 `sourcing_scheduler.py`/`sourcing_score.py`/`compliance.py`；新增投放转化维度 |
| 素材收集 | 复用 `playwright_kaogujia.py`/`playwright_youmi.py`；新增视频采集（TikTokDownloader 封装） |
| 素材优化 | 复用 `image_generation.py`/`image_review.py`；新增 ffmpeg 视频二创 + 文案管线 |
| 上架 | 新增 `adapters/wechat_openapi.py`；现有 181KB 脚本降级兜底 |
| 投放 | 新增 `services/shop_ads/`（计划/会话/执行/调度/报表）+ `adapters/playwright_shop_ads.py` |
| 基础 | 复用 `workflow_runner.py` 队列、`pricing.py`、`listing_gate.py`、前端框架 |