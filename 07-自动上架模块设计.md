# 07 自动上架模块设计

> **双轨制**：主链路走微信小店官方 OpenAPI（`channels`），现有 181KB Playwright 脚本降级为「官方接口未覆盖操作」的兜底。

## 一、双轨策略

| 轨道 | 用途 | 稳定性 | 写操作 |
|---|---|---|---|
| 官方 OpenAPI（主） | 创建 SPU/SKU、传图、资质、库存、改价、查询状态 | 高（接口契约稳定） | 幂等 + 限额队列 |
| Playwright UI（兜底） | 官方未覆盖的复杂表单（展示类目联动、部分参数、限购设置等） | 低（选择器脆弱） | 降低频次，出错转人工 |

> 原则：能走 API 绝不走 UI；UI 只处理 API 覆盖不到的尾巴；UI 失败不阻塞队列（现有 `page_changed` 容错机制保留）。

## 二、上架流水线

```
选品商品池（完成 1688 询价 + 淘宝素材 + 生图 + 图片审核）
  → 上架前校验（见下清单）
  → 创建 SPU（标题/类目/资质/运费模板/限购）
  → 创建 SKU（逐 SKU 售价=定价阶梯结果，库存=10000，限购默认每月 2 件）
  → 上传主图（5 张 1:1）/详情图
  → 提交平台审核
  → 轮询审核状态 → 拿到平台商品 ID + 可访问链接
  → 仅当链接真实可访问 → 状态置「已上架」
```

## 三、上架前校验清单（硬门禁，失败不入队）

- [ ] 标题 15–35 字符，非虚构承诺
- [ ] 类目准确 + 资质完整
- [ ] 主图 ≥5 张 1:1 且不全相同；详情图完整
- [ ] 逐 SKU 真实成本（1688 订单确认页询价）+ 差异化售价已生成
- [ ] 必填购买设置完整（限购/物流/售后）
- [ ] 合规预审通过（品牌/功效/供应链词）

## 四、OpenAPI 薄封装（新增 `adapters/wechat_openapi.py`）

```
WechatOpenApiAdapter
  ├─ _sign(payload)                 # SHA256 签名 + 时间戳窗口
  ├─ _call(method, biz, retry)      # 统一调用 + 限额退避 + 幂等重试(3次)
  ├─ create_spu / update_spu
  ├─ create_skus / update_stock / update_price
  ├─ upload_image（腾讯云 COS 直传，返回素材 ID）
  ├─ submit_audit / query_audit_status
  └─ get_product_link              # 已上架商品可访问链接
```

配额管理：按接口维度令牌桶，写入请求排队；失败分类复用 `WorkflowJob` 错误码表（AUTH_REQUIRED / RATE_LIMIT / TIMEOUT / PLATFORM_REJECT）。

## 五、状态机与证据

```
待上架 → 创建中 → 草稿 → 提交审核 → 平台审核中 → 已上架(含真实链接)
                                ↘ 审核驳回 → 平台拒审处理（platform_rejection.py：记录原因+自动修复候选）
```

- 状态只允许「拿到真实可访问链接」后标记已上架（沿用原项目铁律）
- 每次操作留痕：`UploadHistory` + `WechatUploadLog`，前端「可上架品」点击后必须显示任务 ID/阶段/错误，不静默消失

## 六、与投放的衔接

- 上架成功的商品自动出现在「小店投放·添加托管商品」候选池（仅展示销售中商品）
- 上架时间与托管节奏错峰，避免同时批量提交触发平台风控