# 05 自动收集素材模块设计

> 目标：持续收集**视频素材（9:16，投放/投稿用）**与**图片素材（主图/详情，上架用）**，去重、标准化、标签入库，供素材优化与投放直接消费。

## 一、素材来源

| 类型 | 来源 | 方式 | 状态 |
|---|---|---|---|
| 视频 | 视频号达人/热门视频 | 自研采集器（签名+直链） | 需自建 |
| 视频 | 抖音/快手/小红书 | `TikTokDownloader` 二次封装（批量关键词/达人主页） | 开源复用 |
| 视频 | 淘宝/1688 商品视频 | Playwright 同步浏览器抓取 | 半成品可扩展 |
| 图片 | 淘宝/1688 同款图 | 已实现（`fetch_taobao_references.py`/`fetch_1688_images.py`） | 半成品已验证 |
| 图片 | 考古加/有米云榜单图 | 采集候选时顺带缓存（`IMAGE_CACHE`） | 半成品已验证 |

## 二、采集流水线

```
定时采集任务（调度器账本/节流/熔断）
  → 下载（素材下载中台：多实例 HTTP API，参考 Douyin_TikTok_Download_API）
  → 去重：视频 MD5 + 感知哈希（关键帧）；图片 phash
  → 标准化：ffmpeg 转码为规范格式（见下）
  → 标签化：类目/来源/达人/时长/清晰度/热度字段
  → 入库：素材库（MinIO 存储 + PostgreSQL 索引）
  → 评估标签（探索期/高效/潜力）由投放回读结果回流
```

## 三、素材硬性规格（写死，投放/投稿共用）

| 项 | 要求 |
|---|---|
| 分辨率 | ≥720×1280 |
| 比例 | 9:16（竖屏） |
| 格式 | MOV / MP4 |
| 大小 | ≤500M |
| 时长 | 5 ~ 300 秒 |

> ffmpeg 输出参数锁定示例：`ffmpeg -i in.mp4 -vf scale=720:1280:force_original_aspect_ratio=decrease,pad=720:1280:(ow-iw)/2:(oh-ih)/2 -t 300 -c:v libx264 -crf 23 -c:a aac output.mp4`（实际按素材源微调）

## 四、素材库表（新增）

```python
class Asset(Base):              # 素材库统一实体
    id
    asset_type                      # video / image
    source_platform                 # video号/抖音/快手/淘宝/1688/...
    source_url
    md5, phash                      # 双去重
    file_path (MinIO 键)
    duration, resolution, size      # 视频元数据
    tags_json                       # 类目/达人/场景标签
    heat_score                      # 来源热度（播放/点赞/销量参考）
    evaluation                      # 探索期/高效/潜力（投放回写）
    upload_status                   # 本地/已传小店素材库
    platform_material_id            # 小店素材库 ID（投放绑定用）
    created_at, updated_at
```

## 五、合规模块

- 素材版权：只采集「可二创」来源；搬运素材进入素材库前标记来源与二创义务（去水印/加混剪/换文案）
- 内容预审：复用 `compliance.py` 供应链词/品牌词过滤，避免「1688/工厂/源头」字样进入素材
- 平台要求：素材评估被平台判「审核不通过或源文件损坏」的自动下架（截图确认：此类素材投放时不支持选择）