# M2 自动收集素材 · 决策记录（decisions）

> 记录本模块关键技术决策：决策内容、理由、备选方案、日期、决策人。

| 日期 | 决策 | 理由 | 备选方案 | 决策人 |
|---|---|---|---|---|
| 2025 体系建立日 | 09 文档新增表 `assets` 在本模块实现为 `asset_items`（前缀 asset_*） | 宪法第 4 节"模块内新增表一律加模块前缀"，避免与基座/其他模块表冲突；字段与 05 文档第四节完全对齐 | 直接命名 `assets`（违反前缀纪律） | 总工 |
| 2025 体系建立日 | 视频号采集不依赖 TikTokDownloader，自研采集器（页面层 + signer.py 直链解析层接口化） | TikTokDownloader 对视频号支持弱（R-M2-05）；视频号直链需签名解析且随版本变化，接口化可单点替换 | 强行依赖 TikTokDownloader 视频号分支（不可靠） | 总工 |
| 2025 体系建立日 | 素材存储抽象存储接口（LocalStorage → MinIOStorage），当前 file_path 用本地相对键 | MinIO 暂缺（R-M2-22），M4 才随全局迁移对象存储；抽象接口让 M4 迁移只改一行配置 | 直接硬编码本地路径（M4 迁移成本高） | 总工 |
| 2025 体系建立日 | 评估标签枚举码：exploring/efficient/potential（探索期/高效/潜力），M2 不主动写，仅 M5 回写并落 `asset_evaluations` 审计 | 跨模块口径唯一（R-M2-13）；审计留痕满足宪法第 8 节"操作留证据" | 中文直接入库（跨模块易歧义） | 总工 |
| 2025 体系建立日 | 下载中台 v0.1（子代理 F）：下载核心与 ORM 表解耦，通过 `DownloadJobRepo` 协议注入（enqueue/get_job/list_jobs/claim_next/finish_success/finish_failure/retry_job/release_claim/source_risk_control/set_source_risk_control），默认实现 `SqlAlchemyDownloadJobRepo` 延迟导入 `backend.materials.repo.AssetRepo`（未就绪时方法调用给清晰报错，集成验收由总工补跑） | 并行安全：D 在建表与 AssetRepo，F 不能等；fake repo 保证单元测试零 DB 依赖（R-M2-17） | 直接依赖 D 的 ORM（并行阻塞、耦合） | 子代理 F |
| 2025 体系建立日 | 错误码→退避基表：RATE_LIMIT 180s / TIMEOUT 60s / NO_MATCH 120s / 其他 60s；节流级 0~4 ×1/2/4/8/16（next_run_at=now+base×2^level）；AUTH_REQUIRED/VERIFICATION_REQUIRED 不自动重试转 `blocked` 等人工（P-002） | 对齐 09 错误码体系与 R-M2-06 退避策略 | 统一退避不分类（频控误伤） | 子代理 F |
| 2025 体系建立日 | 熔断：worker 内存连续失败计数 ≥2 即镜像写 `asset_sources.risk_control=1`（repo 支持时），冷却期（circuit_breaker_cooldown_seconds 默认 300s）内 claim 到的该平台任务放回队列（release_claim），冷却后自动放行一个任务当探针，成功即恢复 risk_control=0 | 对齐 R-M2-04/R-M2-21 连续失败≥2 熔断 + 探针恢复 | 仅内存熔断不持久化（进程重启熔断丢失） | 子代理 F |
| 2025 体系建立日 | 断点续传两级：`fetch_file` HTTP Range 续传（416/文件变更→放弃部分文件全量重下）；worker 按 job_id 固定 `.part` 临时文件，失败保留供下次续传，成功入库后清理 | R-M2-06 断点续传 + 宪法第 8 节断点续跑 | 每次全量下载（流量浪费、易超时） | 子代理 F |
| 2025 体系建立日 | 入队幂等口径：`source_url` 已存在且 status≠success 即视为未完成，返回既有任务（200 existing=true）；成功后可再入队新建。失败任务用 `POST /jobs/<id>/retry` 重置 | 幂等防重复（宪法第 8 节）+ 失败可重试 | 仅 queued/running 判重（failed 无法再入队） | 子代理 F |
| 2025 体系建立日 | 下载中台 HTTP API 用 Python 标准库 `http.server.ThreadingHTTPServer`，零新增依赖（requirements 无 FastAPI）；实例标识 WORKER_ID 环境变量（默认 hostname-随机后缀），多实例并行领任务靠租约互斥 | requirements 已定无 FastAPI；多实例靠 45min 租约 + lease_owner 隔离（R-M2-07） | 引入 FastAPI（违反零新增依赖约束） | 子代理 F |
| 2025 体系建立日 | `finish_success(id, file_path, md5, size, evidence)` 的 file_path/md5/size 在 SQL 实现中写入 evidence_json（asset_download_jobs DDL 无这三个独立列）；asset_id 回填由 AssetRepo 后续处理（不在下载中台职责内） | DDL 权威（database/README.md），不改表结构；内存 fake 版额外保留同名字段便于测试断言 | 给 asset_download_jobs 加列（改 DDL，需与 D/总工协调，v0.1 不做） | 子代理 F |
| 2025 体系建立日 | POST /jobs 的 priority 仅接受不落库（DDL 无 priority 列），v0.1 队列按 id 序领取；若后续要优先级排队需改 DDL | DDL 权威 | 内存 fake 按 priority 排序（与 SQL 行为不一致，v0.1 不做） | 子代理 F |
| 2025 体系建立日 | 熔断默认阈值对齐任务书「连续失败≥2」：将 D 的 config.py `circuit_breaker_failures` 默认由 3 改为 2（risks.md R-M2-04/R-M2-21 亦写 ≥2）；worker 始终读配置值，测试显式注入 2 | 任务书/风险清单口径一致 | 维持 3（偏离任务书口径） | 子代理 F |
| 2025 体系建立日 | ★总工裁定：子代理 F 对 config.py 的改动 `circuit_breaker_failures` 3→2 **予以通过**（不改回） | 09 文档第三节「连续失败 ≥2 → risk_control」与 risks R-M2-04/R-M2-21 口径一致；与 sourcing 基线（SchedulerConfig.circuit_breaker_failures=2）对齐 | 维持 3（偏离全项目口径） | 总工 |
| 2025 体系建立日 | ★总工裁定：下载中台 HTTP API 默认端口由 8787 改为 **8788**（`backend/materials/__main__.py`），并登记全局踩坑日志 P-008 | 8787 已被工作区另一服务 captcha-vision-gateway 占用（WinError 10013，子代理 F 实测）；改默认避免后续实例撞端口 | 维持 8787 由总控协调占用方（依赖外部动作） | 总工 |
| 2025 体系建立日 | 双去重器（子代理 E）：视频 phash 指纹值存储格式定为 **JSON 数组字符串**（`["帧0hex","帧1hex","帧2hex"]`，数组下标 = 首/中/尾帧序号即「phash 含帧标识」），与 `asset_items.phash`（TEXT，视频=关键帧 phash JSON）共用同一 combined 值；`image_phash` 指纹为纯 16 位 hex；检查器读取兼容 `{index}:{hex}` 与纯 hex（前向兼容） | 与 database/README.md DDL「phash 含帧标识」及 context 1.2「存 JSON 数组或主帧值」对齐；repo.create_asset 已按 combined 值注册 video_phash 指纹（既有测试口径），去重器必须与之互通 | 每帧独立注册 `{index}:{hex}` 多行（与 create_asset 单行 combined 注册不兼容，弃用） | 子代理 E |
| 2025 体系建立日 | 去重阈值：沿用 `DedupConfig.phash_hamming_threshold` 默认 **8**（与 sourcing 一致）；边界单测锁定「距离 8 判重、9 不判重」；服务级 `_find_approx` 用合成指纹验证 8/9 边界 | 阈值配置化（R-M2-11）且离线 fixtures 校准前不改默认；与 sourcing/dedup.py 同为 8，跨模块判重口径一致 | 调低至 6/7 或调高（需真实素材样本校准后再定，当前无样本库） | 子代理 E |
| 2025 体系建立日 | 视频关键帧抽取用 `FrameExtractor` 抽象可插拔：`FFmpegFrameExtractor`（真实实现，二进制解析顺序 构造参数 > MATERIALS_FFMPEG_PATH > PATH；缺失抛 `FFmpegNotFoundError` 含修复指引）+ `MockFrameExtractor`（测试注入固定帧）；测试全程零真实 ffmpeg 依赖（本机未安装，已探测） | R-M2-15（ffmpeg 缺失不静默、清晰报错）与 R-M2-17（测试零环境依赖）；真实抽帧逻辑环境就绪后自动可用无需改代码 | 测试直接调真实 ffmpeg（本机缺失即测试全崩，违反 R-M2-17） | 子代理 E |
| 2025 体系建立日 | ffmpeg 标准化器（子代理 C）采用「可插拔 Runner 抽象 + mock 优先」交付：`FFmpegRunner`（probe/transcode）抽象，真实实现 `FFmpegProcessRunner`（subprocess.run，超时配置化），测试注入 `MockFFmpegRunner`；本机 ffmpeg/ffprobe 未安装（已探测），全部功能与测试以 mock 模式交付，环境就绪后**无需改代码**即切真实实现；真实转码用例 `pytest.mark.skipif(not detect_ffmpeg())` 保护，环境就绪后自动启用 | 本机 ffmpeg 缺失（R-M2-15 环境事实），总控指示「先实现 + 测试用 mock，环境就绪后切换」；零真实 ffmpeg 依赖（R-M2-17） | 等环境就绪再开发（阻塞全模块交付） | 子代理 C |
| 2025 体系建立日 | `NormalizeConfig` 定义为**嵌套 BaseSettings**（env_prefix=MATERIALS_），`MATERIALS_FFMPEG_PATH`/`MATERIALS_FFPROBE_PATH` 直接映射到 `normalize.ffmpeg_path`/`normalize.ffprobe_path`（已实测 pydantic-settings 2.15：嵌套 BaseSettings 可读环境变量，普通嵌套 BaseModel 不读）；`ratio_tolerance` 默认 0.01，与 `normalizer.validate_specs` 模块常量 `RATIO_TOLERANCE` 同值 | 任务书要求 config 新增 normalize 子配置且只追加不改既有项；容差/转码参数集中配置便于按素材源微调（宪法第 8 节配置化） | 顶层平铺字段（污染既有配置命名空间） | 子代理 C |
| 2025 体系建立日 | 转码命令对齐 05 文档第三节锁定示例：`-vf scale=720:1280:force_original_aspect_ratio=decrease,pad=720:1280:(ow-iw)/2:(oh-ih)/2 -t 300 -c:v libx264 -crf 23 -c:a aac`，参数从 `config.normalize` 构建（video_filter/duration_limit/crf 等），runner 层 `SPEC_DEFAULTS` 兜底且与之同值；ffmpeg/ffprobe 缺失 raise `NormalizerError`（错误信息含「ffmpeg/ffprobe」+ 安装指引，R-M2-15 不静默）；转码/探测超时也 raise `NormalizerError`（R-M2-16 资源占用保护） | 参数集中便于微调；缺失即清晰报错防静默（R-M2-15/P-007） | 参数散落各调用点（改一处漏一处） | 子代理 C |
| 2026-08-28 | ★子代理 A：TikTokDownloader 二次封装按「锁定版本设计封装 + fixtures 测试」落地：封装完整实现（check_available / search_download / author_download + 安装校验 + 版本锁定逻辑），真实二进制**不安装不下载**（本机未装，R-M2-17）；锁定 CLI 契约 `--mode search/author --target X --count N --output DIR [extra_args]`（真实版本对接时按所装版本语法核对 build_command）；错误分类对齐 downloader.py 码表（TIMEOUT / RATE_LIMIT / AUTH_REQUIRED 不自动重试转人工 P-002 / PLATFORM_REJECT / NO_MATCH / UNEXPECTED）；脱敏扩展 downloader.redact_url 键集（补 sec_uid/uid/user_id 等账号类参数，urlencode 的 %2A%2A%2A 还原为可读 `***`）；fake .py binary 用当前解释器启动（fixtures 模式，真实 exe 不受影响）；文本解析字段顺序无关（文件名行不再丢弃先行的标题/作者/链接）；平台开关 `config.tiktok.enabled` 在 author_download 按达人 URL 平台校验（R-M2-21） | 任务书环境事实「TikTokDownloader 未安装 → 锁定版本设计封装 + fixtures 测试」；risks R-M2-04/R-M2-05/R-M2-17；P-004 脱敏纪律 | 直接对接真实 CLI 并联网安装（本机不可装、不可测，违反任务书） | 子代理 A |
| 2026-08-28 | ★子代理 B3：榜单图缓存 BoardImageCache 落地（`backend/materials/collectors/board_image_cache.py` + 测试 `test_materials_board_image_cache.py`）：①缓存键 `board_cache/{source}/{board_id}/{item_id}.jpg`（组件消毒：非安全字符→`_`、去前导点，LocalStorage._resolve 路径穿越防护双保险；None/空→unknown）；②幂等：已缓存（storage.exists）直接命中返回 `{cached:True, hit:True}` 不重复下载；③批量失败隔离：`cache_board_images` 单条失败记日志 + 收集 `{item_id, error_code}`，不影响其他条（R-M2-09）；④失败分类复用 downloader.classify_http_status：404/410→NO_MATCH、429/403→RATE_LIMIT、401→AUTH_REQUIRED、其他 4xx→PLATFORM_REJECT、超时→TIMEOUT、网络/存储/其余→UNEXPECTED；**本类任何异常不抛出**（缓存失败不影响选品主流程）；⑤多源接口化：sources 白名单默认 `["youmi"]` + `register_source(name)` 动态注册，kaogujia（考古加）仅预留注释（M1 REC-006 考古加降级可选第四源未开发，落地后由上层注册，零硬编码依赖）；⑥config 追加 `board_cache` 子配置（cache_dir 默认 `data/board_cache`、enabled、sources、timeout_seconds、max_bytes 大小上限）只追加不改既有项；enabled=False 为总开关不发起下载（对齐 tiktok.enabled 风控开关模式）；⑦CLI 追加 `board-cache` 子命令（`python -m materials board-cache --source youmi --board B1 --json '[...]'`，可选 --cache-dir），不覆盖既有子命令；⑧测试全走本地 http.server（标准库线程）零外网零登录态（R-M2-17），25 用例锁定缓存键/幂等/失败隔离三类关键行为 | 任务书 M2 B3 + R-M2-09 + context 2.4；M1 REC-006 考古加未实现 → 多源接口化预留；R-M2-17 fixtures 离线模式 | 依赖考古加采集器实现后再做缓存（考古加未开发，阻塞）；真实有米云下载需登录态环境（本机未确认，仅留接口） | 子代理 B3 |
| 2026-08-28 | ★子代理 B1：视频号采集器（自研，R-M2-03/R-M2-05）落地：①signer 接口化——`SignatureProvider.sign(params,url) -> {"headers":{...},"query":{...}}`（两个键恒存在）；`MockSignatureProvider(fixed_query/fixed_headers)` fixtures/测试用；`RealSignatureProvider(config)` 未校准前 sign() 一律 raise NotImplementedError（不留假算法，验收第 3 条）；②采集器 `WechatVideoCollector`：login_state（无浏览器返回 {logged_in:False,error} 不抛，R-M2-01 隔离）、list_hot_videos（fixtures 读 JSON / auto Playwright 共享 Chrome 骨架，选择器全配置化 P-003，输出字段 {source_platform:"视频号",source_url,source_author,title,heat_score,video_id}，热度降序）、resolve_direct_url（signer 注入 query；Real 未实现→PLATFORM_REJECT；auto 直链为空→PLATFORM_REJECT）；③错误分类对齐 downloader.py 码表：登录门→AUTH_REQUIRED（P-002 转人工）、页面结构变化→PLATFORM_REJECT、无结果→NO_MATCH、超时→TIMEOUT；④config 只追加 `wechat_video` 子配置（enabled/cdp_port 默认 9223 以 sourcing 为准/profile_dir=shared/fixtures_mode 默认 True/boards/selectors，同 TikTokConfig 模式 validation_alias 完整环境变量名）；⑤CLI 只追加 `wechat-collect`（--mode fixtures|auto --limit --board --with-direct-url），并在 cli() 入口统一 stdout/stderr 为 UTF-8（宪法第 11 节，Windows 管道默认 GBK 实测乱码）；⑥fixtures `backend/fixtures/materials/wechat_video_hot.json`（6 条样本含 direct_url）；⑦auto 模式 URL/选择器/JS 均为**占位**（待共享浏览器登录态 + 抓包校准，校准只改 config.selectors 与 signer.py，不崩采集器） | 任务书 B1 + R-M2-03/R-M2-05 + context 2.2；零浏览器零登录态可跑通全链路（R-M2-17）；auto 骨架 + fake page 注入测试覆盖全部错误分支（零真实浏览器） | 依赖 TikTokDownloader 视频号分支（R-M2-05 已否决）；auto 模式本任务连真实浏览器验证（环境事实②未确认登录态） | 子代理 B1 |
| 2026-08-28 | ★子代理 B4-1：素材标签化与内容合规预审落地（`backend/materials/tagger.py` + 测试 `test_materials_tagger.py`）：①`generate_tags(source_platform, source_author, title, category_hint, max_tags=None, config=None)`——平台标签（映射表，未知平台原样保留）/达人标签（截断 30 字）/类目标签/标题关键词（`[^\w]+` 去标点切分、停用词剔除、片段截断 12 字、去重保序），总数上限 max_tags 可配（None→config.tagger.max_tags 默认 8），标题为空不崩；②`MaterialCompliance` 复用 sourcing.compliance 词库（**只 import 引用不复制词表**，模块常量 = sourcing 同名 list 对象同一性验证）；判定优先级 禁售>品牌>供应链（均 reject）>功效（review，需资质人工闸门）>pass，check_type 枚举对齐 asset_compliance_checks 口径（supply_chain_word/brand_word/efficacy_word/prohibited_word/content_precheck）；③`evaluate_and_record` 调 repo.record_compliance_check 留痕（check_type/result/hit_words_json/note——note 含命中理由 + 二创义务 derivation_note + source_platform 上下文，R-M2-18），compliance_status 由 repo 同步（pass→passed/reject→rejected/review 不动保持 pending）；④repo.py **仅追加** `mark_disabled(asset_id, reason)`（幂等，upload_status=disabled + asset_uploads 记 status=disabled 台账 evidence_json 含 reason，R-M2-20），`MaterialCompliance.mark_platform_rejected` 委托之；⑤config.py 仅追加 `tagger` 子配置（max_tags 默认 8、tag_keyword_stopwords 默认小表）；⑥测试 27 用例全零外网零浏览器零 ffmpeg（临时 SQLite + 显式注入 cfg_materials 配置，不读环境/.env） | 任务书 B4-1 + R-M2-18/R-M2-19/R-M2-20 + context 1.1/1.5；词库单一事实源（sourcing.compliance 只读复用，禁复制词表）；证据留痕满足宪法第 8 节；derivation_note 无 repo 更新方法 → 仅进 note 证据（任务书限定 repo.py 只追加 mark_disabled，不新增字段更新接口） | 自建词表（违反词库单一事实源）；给 repo.py 加 update_derivation_note（超出任务书 repo 变更范围） | 子代理 B4-1 |
