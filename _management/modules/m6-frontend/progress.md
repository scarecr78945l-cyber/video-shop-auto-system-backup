# M6 前端控制台 · 进度看板（progress）

> 由总工程师持续维护。迭代版本号规则：v0.1 起。
> 更新：2026-08-29 20:05（子代理④验收通过，197 passed + build exit 0 + 冒烟 37 断言全绿）｜ 状态：**v0.1~v0.7 ✅ → v0.8~v1.0 集成验收派工中**。

## 当前迭代：v0.7（人工闸门工作台 + 异常中心）✅ 100%

| 任务 | 负责 | 进度 | 剩余工作 |
|---|---|---|---|
| [x] 子代理④：/workbench 人工闸门工作台（6 类闸门待办卡片 GATE_DEFS + count=0 置灰 + KillSwitch 一键全停快捷卡复用 + 选品复核内联〔manual_review 列表 + gate-confirm 二次确认 + ProductDetailPanel 抽屉〕） | 子代理④ | 100% | 无 |
| [x] 子代理④：/exceptions 异常中心（error_code 分组统计卡 + status 筛选 + 人工接管 retry〔三类文案：验证码/登录/blocked〕+ 空态） | 子代理④ | 100% | 无 |
| [x] 子代理④：lib/workbench.ts（22 单测）+ GateTodoCard/SourcingReviewPanel/ExceptionCenter 组件 + api 类型对齐 + products/review/listing 四页 query 参数直达 | 子代理④ | 100% | 无 |
| [x] 子代理④：单测 +22（13 文件 197 passed）+ tsc 0 errors + build exit 0 + 冒烟 37 断言（gates/exceptions/retry/gate-confirm 全语义） | 子代理④ | 100% | 无 |
| [x] 总工验收：复跑 197 passed + tsc + build | 总工 | 100% | 无 |

## 排期计划（✅ 总控已批准 2026-08-29）

| 迭代 | 阶段 | 任务 | 可拆子代理 | 完成标准 | 状态 |
|---|---|---|---|---|---|
| v0.2 | API 层 | FastAPI 应用 + 管理后台鉴权 + M1~M5 聚合接口 + pytest | **子代理①**（后端 API 层） | 接口可测、鉴权生效、契约会签 | ✅ **验收通过（75 passed）** |
| v0.3 | 前端工程 | Next.js 脚手架 + 路由 + 登录闭环 + lib/api.ts 重写 + lib/format.ts + lib/enums.ts + lib/workflow.ts 重写 | **子代理②**（前端工程） | 页面可渲染、登录可用、vitest 单测过 | ✅ **验收通过（55 passed）** |
| v0.4 | 页面建设 | 总览看板 + 商品队列 + 素材库页 | **子代理③**（页面建设·批次1） | 数据可展示、口径正确 | ✅ **验收通过（107 passed）** |
| v0.5 | 页面建设 | 上架任务页（9 态）+ 托管看板页 | 子代理③（批次2） | 状态机可视化、托管列对齐 | ✅ **验收通过（150 passed）** |
| v0.6 | 页面建设 | 图片审核工作台 + 素材预审 | 子代理③（批次3） | 审核/预审流程可用 | ✅ **验收通过（175 passed）** |
| v0.7 | 人工闸门 | 闸门工作台（选品复核/上架确认/素材预审/验证码接管）+ 异常中心 + 一键全停 | **子代理④**（人工闸门工作台） | 全流程可操作、操作回写成功 | ✅ **验收通过（197 passed）** |
| v0.8 | 集成 | 端到端联调 + 修口 | 子代理⑤（集成验收） | 联调通过 | 🔄 **已派工（运行中）** |
| v1.0 | 验收 | 全量回归（vitest + pytest）+ 验收报告 + 文档同步 | 子代理⑤ | **验收标准全过：控制台可启动、各页面数据可展示、人工闸门可操作（fixtures/mock 模式）** | 🔄 **并入子代理⑤** |

## 可拆子代理清单（任务书自包含，见 brief.md 第六节）

1. **子代理① 后端 API 层**：`backend/api/` FastAPI + 鉴权 + M1~M5 聚合接口 + `backend/tests/test_api_*.py`；验收 = pytest `--basetemp=".pytest-tmp-m6"` 全绿。
2. **子代理② 前端工程**：`frontend/` 脚手架 + 路由 + 登录 + API 客户端重写 + 口径层（format/enums/workflow）；验收 = `next dev` 可启动 + vitest 单测。
3. **子代理③ 页面建设**：看板/队列/素材/上架/托管页（分批派发，复用旧组件改造）；验收 = 各页数据可展示、口径正确。
4. **子代理④ 人工闸门工作台**：选品复核/上架确认/图片审核/素材预审/验证码接管/异常中心；验收 = 全流程可操作。
5. **子代理⑤ 集成验收**：e2e 联调 + 全量回归 + 验收报告；验收 = v1.0 标准全过。

## 里程碑进度

- 本模块当前完成度：**100%**（v0.1 筹备 ✅；v0.2 API 层 ✅；v0.3 前端工程 ✅；v0.4 批次1 ✅；v0.5 批次2 ✅；v0.6 批次3 ✅；v0.7 人工闸门 ✅；**v1.0 集成验收 ✅ 全部验收标准通过**）
- 前端交付基线：工程底座 ✅ + **8 个业务页全部完成**（总览/商品池/素材库/上架任务/托管看板/审核工作台/闸门工作台/异常中心）+ 登录闭环 + 口径层 + 状态机；**197 vitest passed**；`next build` exit 0；e2e 联调 40 断言全绿
- 后端依赖基线：M0~M5 全量 1227 测试全绿（总控口径，需总控复跑确认）；**M6 API 层 75 passed**（`.pytest-tmp-m6`）；**前端 197 passed**（vitest）
- 状态：**M6 v1.0 交付完成，等总控执行 M0~M5 全量回归 + 备份标签（v0.40+ / m6-v1.0）**；移交清单见 frontend/REPORT.md 第六节

## 总控裁决回填（2026-08-29 已批准，详见 decisions.md）

1. ✅ **排期批准**：v0.2 API → v0.3 前端工程 → v0.4~v0.6 页面 → v0.7 闸门 → v1.0 集成；子代理①已完成验收。
2. 🔄 **DA-011 会签**：已回填校验结果（75 passed）；总控转达 M0~M5 待回传（M0 auth 表契约 L1 / M5 枚举文档漂移 D5 / M4 派生字段 D2·D3 / 错误码备案 L4 / requirements 补依赖 L2）。
3. ✅ **鉴权会话表挂 M0**（AuthStore 接口 + `M6_API_AUTH_MODE=fixtures|m0` 过渡，m0 模式表未落地启动即明确报错——已测）。
4. ✅ **v1.0 验收口径 = fixtures/mock 模式**（不依赖真实登录态/平台数据；真实数据接入为 v1.1 增量）。
5. ✅ **金额对外口径 = 元（float）**（内部存储分不变，API 层 ÷100；M1 元透传；前端只消费元）——已同步 context/risks。
