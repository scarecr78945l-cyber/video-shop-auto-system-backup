# M5 自动小店投放（商品托管） · 进度看板（progress）

> 由总工程师持续维护。迭代版本号规则：每次重要返工/改版 +0.1（v1.0 → v1.1）。

## 当前迭代：v0.1（筹备）

| 任务 | 负责 | 进度 | 剩余工作 |
|---|---|---|---|
| [x] 通读设计文档 08/09/10/11/03 + 宪法 + 踩坑日志 | 总工 | 100% | 无 |
| [x] 撰写任务书 brief.md | 总工 | 100% | 无 |
| [x] 风险预判 risks.md（含投放无API/改版/登录态/素材/ROI/资金/批量/回读 8 专项） | 总工 | 100% | 无 |
| [x] 上下文库 context/README.md（数据字典+后台锚点+止损规则表+跨模块契约） | 总工 | 100% | 无 |
| [x] 数据库规划 database/README.md（ad_* 五表 DDL + 配置项清单） | 总工 | 100% | 无 |
| [x] 制定开发排期与子代理拆解（见下） | 总工 | 100% | 无 |
| [x] 建库执行（backend/data/db/m5-ads.db，5 表，v0.2 落地） | 子代理 | 100% | 无 |

## 当前迭代：v0.2（数据层·已完成）

| 任务 | 负责 | 进度 | 剩余工作 |
|---|---|---|---|
| [x] v0.2 数据层：ad_* 5 表 DDL + repo 层（backend/ads/ 包：config/db/tables/models/repo/__main__ + 测试） | 子代理 bc855a68 | 100% | 无 |
| [x] v0.2 验收：定向 27 passed + 全量 258 passed（5 个失败均为 M0 foundation 既有问题，与本模块无依赖）+ init-db 建库 5 表 | 总工 | 100% | 无 |

## 里程碑进度

- 本模块当前完成度：**30%**（v0.1 筹备 15% + v0.2 数据层 15%）
- 已达成里程碑：**ad_* 表可建** ✅（backend/data/db/m5-ads.db 5 表 + repo 层可测可跑）
- 距离目标还差：执行层 v0.3（Playwright 托管执行器·抽象接口+fixtures 模拟）→ 监控层 v0.4 → 回流层 v0.5 → 集成验收 → 真实实投

## 当前迭代：v0.3（执行层·开发中）

| 任务 | 负责 | 进度 | 剩余工作 |
|---|---|---|---|
| [x] 公共骨架：ui_config.py（ShopAdsUiConfig 选择器/页面/节奏配置）+ interfaces.py（PageOps Protocol + PageChangedError） | 总工 | 100% | 无 |
| [~] 托管执行器：会话管理/CDP 连接抽象/两步操作编排/page_changed 检测 + MockPageOps（backend/ads/executor.py + test_ads_executor.py） | 子代理 ad45ec7a（A2 重派，已运行） | 20% | 子代理开发中；总工验收 |
| [x] 投放设置：目标三选一/ROI 填值/素材绑定（高效>潜力>探索期）/提交校验 + Mock（backend/ads/settings.py + test_ads_settings.py） | 子代理 91f77eec | 100% | 无 |
| [x] 投放设置验收：定向 25 passed（.pytest-tmp-m5）+ 代码抽查（优先级/排除未过审/校验优先级/错误码） | 总工 | 100% | 无 |
| [ ] v0.3 集成：executor ↔ settings 对接（run_batch 编排联调）+ 验收 | 总工 | 0% | 执行器子代理完成后执行 |

## 开发阶段管理方式（总控已确认，2025 体系建立日）

1. M5 总工拥有独立会话，全权管理本模块开发全流程（需求→设计→排期→分派→集成→验收→迭代）。
2. 开发任务一律由总工用 `subagent` 工具创建子代理承担，**每任务一个子代理，任务书自包含**（背景/目标/输入输出路径/验收标准/宪法要点——子代理看不到本会话上下文）。
3. 总工负责架构设计、任务拆解、进度管理、验收与集成；**不得把批量开发任务留给自己写**。
4. 子代理完成后总工必须验收（读产出、跑测试，`pytest --basetemp=".pytest-tmp"`），验收不合格退回修改。
5. 子代理阻塞先由总工判断；判断不了写 `BLOCKERS.md` 结束回合，总控回复后继续。
6. 编码纪律：子代理产出中文文件一律 UTF-8 无 BOM（write/edit 工具；禁止 PowerShell Add-Content/Set-Content/`>` 重定向写中文）。

## 后续排期（v0.2 起，可拆子代理）

| 阶段 | 任务 | 可拆子代理？ | 子代理任务粒度建议 |
|---|---|---|---|
| v0.2 数据层 | ad_* 五表建库 + 迁移 + ORM 模型 + app_config 配置项 | 可拆 1 个子代理 | 「M5 数据层」：写 schema.sql + 执行建库 + 模型类 + 配置读取，验收=DDL 可执行、测试通过 |
| v0.3 执行层 | **托管执行器（Playwright）**：①添加商品 ②投放设置 | 可拆（核心，建议 1~2 个子代理） | 「M5 托管执行器」：会话管理/CDP 连接/两步操作/选择器配置化/page_changed 检测；「M5 投放设置」：目标三选一/ROI 填值/素材绑定/提交校验 |
| v0.4 监控层 | 监控回读 + 止损规则引擎 + 余额检测 + 预算硬约束 + 一键全停 | 可拆 2 个子代理 | 「M5 监控回读」：定时快照入库/解析器/断点补快照；「M5 止损规则引擎」：S1~S8 规则实现 + 四层资金防线 + 全停开关 |
| v0.5 回流层 | 数据回写（选品「投放转化」维度 + 素材评估回流 + review_reason） | 可拆 1 个子代理 | 「M5 数据回写」：与 M1/M3 口径协商、data-audit 登记、回写实现 |
| v1.0 集成验收 | 联调 + 单元/模拟 E2E 全绿 + 文档同步 | 视进度拆 1 个子代理 | 「M5 测试验收」：测试用例补全、`pytest --basetemp=".pytest-tmp"` 全绿 |
| v1.1 实投验证 | 真实小额实投（依赖账号/余额/登录态/素材就绪，11 文档第一节前置） | 不拆 | 总工+总控人工值守，不自动实投 |

> 子代理任务书必须自包含（宪法第 9 节）：背景/目标/输入输出文件/验收标准/宪法要点；总工验收不合格退回修改。
