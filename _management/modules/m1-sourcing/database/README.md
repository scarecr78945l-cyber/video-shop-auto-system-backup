# M1 自动选品 · 模块数据库（database）

> 本模块独立数据库：开发库文件 ackend/data/db/m1-sourcing.db（SQLite，不入 git）。
> 铁律：只操作本模块库；表名前缀见宪法第 4 节；生产切 PostgreSQL 时迁移脚本放本目录。

## Schema（DDL）

（待总工填写：建表语句）

## 迁移记录

| 版本 | 说明 | 日期 |
|---|---|---|
| v0 | 初始 | — |
