# M4 自动上架 · 微信小店 Channels OpenAPI 外部契约（核对稿）

> 依据 REC-003 建立：核对微信小店官方 OpenAPI 契约，写进 context/外部契约并标注来源。
> **核对状态（2026-08-30 更新）**：官方文档目录与商品管理接口清单已 web_fetch 核对（来源 `https://developers.weixin.qq.com/doc/store/shop/API/`），**部分销项**。注意：官方文档路径为 `/doc/store/shop/API/`（非旧稿 `channels/API/`）。接口级字段细节仍需逐页核对。
> 官方文档位置：`https://developers.weixin.qq.com/doc/store/shop/API/`（微信小店板块）。

---

## 一、调用基础

| 项 | 内容 | 来源 | 状态 |
|---|---|---|---|
| 认证 | `access_token`（有效期约 7200s），需缓存 + 提前 5min 预刷新（R2） | context/README 3.1、risks R2 | ✅ 已核对：官方「获取接口调用凭据」「获取稳定版接口调用凭据」接口存在（apimgnt/common） |
| 签名 | 请求体 **SHA256 签名 + 时间戳窗口**；本模块自写薄封装（不依赖社区库，决策 D2） | 07 第四节、01 文档 | **签名参数名/拼接规则/窗口秒数待核对（T2）** |
| 配额 | 按接口限额；令牌桶排队（listing_quota_states）；RATE_LIMIT 180s / TIMEOUT 60s 退避 | 07 第四节、09 错误码表 | ✅ 已核对：官方「查询API调用额度」「重置API调用次数」接口存在；**各接口 QPS/日配额具体数值待核对（T5）** |
| 幂等 | 请求幂等键 `request_id`（= task_id+接口名+序号）；重试 3 次；`(product_id,stage,generation_version)` 唯一约束 | decisions D6、09 | 已知 |
| 金额 | 一律整数「分」（决策 D7） | decisions D7 | 已知 |
| 模式 | `WECHAT_MODE=mock`（默认，REC-004）/ `live`（显式开启且密钥齐备）；**不提交真实商品（铁律）** | REC-004 | 已知 |

## 二、接口清单（官方接口 → 本模块适配器方法映射）

> 2026-08-30 官方文档目录核对：商品管理接口为「添加商品/更新商品/上架商品/下架商品/获取商品/获取商品列表/撤回商品审核/立即开售/发品前校验/获取商品提审限额/新增第三方货源信息」等（channels-shop-product 域）；资源上传为「上传图片/上传资质图片/视频分块上传」；库存为「获取库存/快速更新库存」。**与适配器方法的映射及字段细节待逐页核对（T3/T4）**。

| 官方接口 | 本模块适配器方法 | 用途 | 状态 |
|---|---|---|---|
| 添加商品（channels-shop-product/shop） | `create_spu`+`create_skus`（或合并） | 建商品主体+SKU（官方可能一次创建） | **字段映射待核对（T4）** |
| 更新商品 | `update_spu` | 改商品主体 | 待核对 |
| 上架商品 | `submit_audit` 后置 + 上架 | 提交审核→上架 | 待核对 |
| 获取商品 / 获取商品列表 | `query_audit_status` | 审核状态/商品信息回读 | 待核对 |
| 撤回商品审核 | — | 审核撤回 | 待核对 |
| 商品立即开售 / 取消开售 | — | 定时开售控制 | 待核对 |
| 发品前校验 / 获取商品提审限额 | `listing_gate` 前置 | 校验/限额查询 | 待核对 |
| 上传图片 / 上传资质图片（apimgnt/resource） | `upload_image` | 传图返回素材 ID | **COS 直传 vs 官方上传接口待核对（T6）** |
| 获取库存 / 快速更新库存（stock） | `update_stock` | 库存读写 | 待核对 |
| 获取商品H5短链 / 商品二维码 / Scheme码 | `get_product_link` | 已上架链接（R22 判据） | 待核对 |
| 获取账户余额 / 资金流水（funds） | — | M5 余额检测可复用 | 已核对存在 |

## 三、错误码映射（平台错误 → WorkflowJob 码表）

| 平台错误场景 | 映射 | 重试 | 来源 |
|---|---|---|---|
| 登录/token 失效（40001/42001 等） | `AUTH_REQUIRED` | 不重试 → 人工 | context 3.3（**平台错误码具体编号待核对 T7**） |
| 验证码/安全验证 | `VERIFICATION_REQUIRED` | 不重试 → 人工 | 同上 |
| 限流/频繁调用 | `RATE_LIMIT` | 180s 退避 | 同上 |
| 超时/网络 | `TIMEOUT` | 60s 退避 | 同上 |
| 资质/内容/参数驳回 | `PLATFORM_REJECT` | 记录原因 → 修复候选/人工 | 同上 |
| 其余 | `UNEXPECTED` | 60s 退避，留证据 | 同上 |

> 官方「公共错误码」文档存在（dev_before/commErrCode.html）——T7 可据此核对。

## 四、待核对清单（官方文档核对后逐项销项）

| # | 待核对项 | 影响 | 核对方式 | 状态 |
|---|---|---|---|---|
| T1 | access_token 获取接口路径与参数（grant_type/appid/secret） | P1 `_get_token` 实现 | ✅ 接口存在（apimgnt/common/api_getaccesstoken） | 参数细节待核对 |
| T2 | SHA256 签名参数名、拼接顺序、时间戳窗口秒数 | P1 `_sign` 实现 | 官方文档 | 待核对 |
| T3 | 各接口实际请求路径与方法（GET/POST） | P1 `_call` 路由 | ✅ 目录已确认域（channels-shop-product 等）；具体路径待逐页 | 部分销项 |
| T4 | 各接口入参/出参字段名与类型 | P1 方法签名 | 官方文档逐页 | 待核对 |
| T5 | 各接口配额（QPS/日上限） | 令牌桶参数 | ✅ 配额查询接口存在；数值待核对 | 部分销项 |
| T6 | upload_image 上传协议（官方上传接口 vs COS 直传） | P1 `upload_image` | ✅ 官方「上传图片」接口存在；协议细节待核对 | 部分销项 |
| T7 | 平台业务错误码编号表 | 错误分类映射 | ✅ 官方「公共错误码」文档存在 | 待核对 |

> 核对完成一项即在对应行标记「已核对」并补充来源 URL；**P1 骨架开发不等待核对**（按上表已知项实现，待核对项用常量占位 + 注释「待核对」）。
