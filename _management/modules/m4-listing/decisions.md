# M4 自动上架 · 决策记录（decisions）

> 记录本模块关键技术决策：决策内容、理由、备选方案、日期、决策人。
> 只追加，不改写历史条目；代码行为与设计文档不一致时须在此登记（宪法第 8 节 6）。

| 日期 | 决策 | 理由 | 备选方案 | 决策人 |
|---|---|---|---|---|
| 2025 体系建立日 | D1 上架双轨制落地：官方 OpenAPI（channels）为主链路，Playwright UI 仅兜底官方未覆盖操作（展示类目联动、部分参数、限购设置），UI 失败不阻塞 OpenAPI 队列 | 07/03 文档既定；接口契约稳定，UI 改版不再致命（P-003） | 纯 UI 自动化（否决：改版即崩，历史上架脚本已证明） | 总工 |
| 2025 体系建立日 | D2 OpenAPI 自写薄封装（`adapters/wechat_openapi.py`），**不依赖**社区语言封装库 | 01 文档结论：社区库多人维护、微信接口变更后更新慢；官方文档自写约几百行，可控 | 依赖 Go/Python/Node 社区封装（否决：变更失效风险高） | 总工 |
| 2025 体系建立日 | D3 一模块一库：本模块独立库 `backend/data/db/m4-listing.db`，全部表加 `listing_*` 前缀 | 宪法第 4 节 + P-005：防数据污染、防表冲突 | 共用基座库（否决：模块间相互读写脏数据扩散） | 总工 |
| 2025 体系建立日 | D4 `upload_history` / `wechat_upload_logs` 归属基座 M0（半成品既有表，09 文档列为复用）：M4 **默认只读参照**，本模块操作留痕以 `listing_upload_assets` / `listing_op_logs` 为准（字段语义对齐）；待 M0 开通写通道（总控批准）后再切换正式落点，切换时登记 data-audit.md | 宪法第 4 节：共享表写入须总控协调；避免跨库写依赖阻塞 M4 开发 | M4 直接写基座表（否决：破坏共享表写通道管控） | 总工 |
| 2025 体系建立日 | D5 「已上架」唯一判据 = `get_product_link` 返回 + 链接 HTTP 可达（真实链接验证铁律）；任何内部状态/本地猜测都不允许标记 `listed`，单测固化该断言 | 07 文档与原项目铁律；防止前端误显已上架、销售承诺落空（risks R22） | 审核通过即标记已上架（否决：链接可能不可访问） | 总工 |
| 2025 体系建立日 | D6 幂等键设计：`(product_id, stage, generation_version)` 唯一约束 + 请求幂等键 `request_id`（= task_id + 接口名 + 序号），创建前按平台商品 ID 反查去重 | 09 文档：防断点续跑/重试导致重复创建 SPU/SKU（risks R6） | 仅靠平台幂等（否决：平台侧幂等语义不保证覆盖全部接口） | 总工 |
| 2025 体系建立日 | D7 金额口径统一：售价/成本一律 **INTEGER 单位「分」** 入库与传递 | 避免浮点误差；跨模块口径一致（宪法第 5 节） | 元 + 两位小数 DECIMAL（否决：Python 浮点/JSON 传递易失精度） | 总工 |
| 2025 体系建立日 | D8 配额管理：按接口维度**令牌桶**（`listing_quota_states`）+ 连续失败 ≥2 熔断该接口（探针恢复） | 07 文档：写入请求排队；避免某接口限流拖垮全局 | 全局限速（否决：读接口与写接口互相拖累） | 总工 |
| 2025 体系建立日 | D9 错误分类**完全复用** WorkflowJob 码表（VERIFICATION_REQUIRED / AUTH_REQUIRED / RATE_LIMIT / TIMEOUT / NO_MATCH / PLATFORM_REJECT / UNEXPECTED），不新增私有错误码 | 宪法第 8 节 3 + 09 文档：队列/前端/人工接管统一识别 | 私有码表（否决：跨模块识别成本高） | 总工 |
| 2025 体系建立日 | D10 拒审自动化边界：`platform_rejection.py` 只产出**自动修复候选**（改标题/重传图/补资质/改价），重新提交必须过二次门禁；自动修复不可行 → `manual` 人工处理 | 10 文档人工闸门：合规兜底，防违规商品自动重提循环 | 驳回后自动无限重提（否决：拒审风暴与风控风险） | 总工 |
| 2025 体系建立日 | D11 P1 薄封装实现形态：**mock 模式为默认且唯一可运行模式**（9 接口内置 fixture、零网络），live 模式以 TODO 占位（`UNEXPECTED` 报错并注明依赖待核对项 T1/T2），签名参数/时间戳窗口用占位实现并注释「待核对 Tn」 | REC-004（资质未确认，绝不提交真实商品）+ REC-003（不阻塞骨架，待核对项占位）；web_search 额度不足（外部契约无法即时核对） | 直接实现 live 模式（否决：官方契约未核对前臆造字段风险高） | 总工 |
| 2025 体系建立日 | D12 子代理派发策略：P1 四次派发三次中断零产出（含 web_search 额度不足、上下文耗尽）后，第四次采用**全内联契约任务书 + 全新会话**成功；后续子代理任务书一律全内联关键契约、压缩必读文件清单、强制「第一动作写盘」 | 前三派失败模式分析：子代理在阅读/搜索阶段消耗回合后中断导致零产出；内联契约可大幅降低中断损失 | 总工亲自实现全部开发任务（否决：违反宪法 9.2 批量任务下放；仅在子代理多次失败且任务为核心骨架时部分适用） | 总工 |
| 2025 体系建立日 | D13 DA-009 集成修复：pipeline 在 create_spu/create_skus/submit_audit 后**落库 listing_spus/listing_skus**（幂等 upsert，ON CONFLICT 更新），并回填 SPU audit_id/status；repo 新增 upsert_spu/upsert_skus/get_spu/get_skus；M5 候选池 title/category/价格不再恒 None | M0 A7 集成冒烟发现缺口：SPU/SKU 平台映射未写本库，候选池关联查询字段恒空；落库对齐 database/README.md 表结构（幂等，断点续跑重跑不重复插入） | 候选池改为任务表冗余字段（否决：破坏 listing_spus/listing_skus 单源设计，重复存标题/价格） | 总工 |

## 待总控裁定

| # | 待决事项 | 影响 | 建议 |
|---|---|---|---|
| PEND-1 | 官方 channels OpenAPI 契约核对（签名参数名/接口路径/配额阈值/access_token 获取方式）——本模块无官方文档权限，需总控协调获取权威文档 | P1 薄封装定稿前置条件（risks R5/R4） | 总控提供官方文档或截图后，P1 子代理按契约实现并登记偏差 |
| PEND-2 | 企业主体/类目资质开通状态（个体户 or 企业；现 9 类类目白名单） | R1 准入体检参数 | 待用户/总控确认后写入 app_config 类目白名单 |
