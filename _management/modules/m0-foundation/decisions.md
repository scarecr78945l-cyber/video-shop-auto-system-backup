# M0 基座与数据治理 · 决策记录（decisions）

> 记录本模块关键技术决策：决策内容、理由、备选方案、日期、决策人。文档与代码不一致时必须记入此处（宪法第 8 节）。

| 日期 | 决策 | 理由 | 备选方案 | 决策人 |
|---|---|---|---|---|
| 2026-08-28 | 共享基座表（workflow_jobs/tasks/logs/app_config/error_codes）无前缀、归属 M0，其他模块表带各自前缀（m1_/asset_/image_/ad_…），共享表全员只读 | 宪法第 4 节「表名前缀 + 共享表只读」；避免业务模块表与基座表冲突 | 全表统一前缀 base_ | M0 总工 |
| 2026-08-28 | `workflow_jobs` 幂等键采用 `(product_id, stage, generation_version)` 唯一约束 | 09 文档明确；防并发重复入队/重复上架 | 仅 (product_id, stage) | M0 总工 |
| 2026-08-28 | 错误码表独立建表 `error_codes`（code/retryable/backoff_seconds/action），不并入 `app_config` JSON | 重试策略需结构化查询与种子数据，独立表可被调度器/队列直接 JOIN 引用；错误码是全系统唯一权威（宪法第 8 节） | 全部放 app_config 键值 | M0 总工 |
| 2026-08-28 | 金额单位统一为「元」Float，平台 API 以「分」计处由适配层换算（字段名注明 `*_fen`） | 与现有 pricing.py/sourcing 口径一致，最小改动；跨模块报表口径统一 | 全系统改用「分」整数 | M0 总工 |
| 2026-08-28 | `tasks`/`workflow_jobs` 以迁移包实际 DDL 为准：首轮只规划骨架，迁移包（`app.sanitized.db`→`backend/app.db`）落地后立即核对修订 | 当前 backend 树无 workflow 表（迁移包有 40 条 workflow_jobs/939 tasks），避免凭空设计造成迁移冲突 | 完全重设计表结构 | M0 总工 |
| 2026-08-28 | pytest 一律 `--basetemp=".pytest-tmp"`，已更新 `backend/README.md` 测试命令 | P-001 本地临时目录 WinError 5；防复发措施需在文档落实 | 修改系统 TEMP 权限 | M0 总工 |
| 2026-08-28 | 采纳总控裁决 REC-005（DA-001）：金额一律「分」int 存储（含 JSON 内金额），展示层转元；时间一律 UTC（ISO8601 带时区）存储、时间戳字段后缀 `_at`、展示层转 UTC+8 | 与微信 channels API/投放后台口径一致，消除 M0 初稿（元/Float）与 M4/M5（分/int）冲突；M0 修订 context 与 database 文档，logs.ts→logs.created_at | 维持「元」并要求 M4/M5 改 | M0 总工（裁决：总控） |
| 2026-08-28 | 时间列用 `AwareUTCDateTime`（TypeDecorator：SQLite 存 naive UTC、读回补 tzinfo 强制 aware UTC；PostgreSQL TIMESTAMPTZ 原生带时区） | SQLite 无时区类型导致 naive/aware 混用（TypeError: can't subtract offset-naive and offset-aware）；治本方案保证 Python 层永远 aware UTC（REC-005） | 测试层统一转 aware（掩盖问题） | M0 总工 |
| 2026-08-28 | `retry_after` 为总控第 1 步指定字段名（语义=下次可重试时间），**命名例外**：不满足 REC-005「时间戳 _at 后缀」规则（以 `ter` 结尾非 `_at`），保留总控命名，测试单独验证其存在性（test_retry_after_retry_time_field） | 总控指示优先；语义清晰（"在此之后可重试"）；改名违背已批准的 DDL | 改名 retry_at / retry_after_at | M0 总工（字段名裁决：总控） |
| 2026-08-28 | 测试用 SQLite 内存库（`sqlite:///:memory:`）时 `db.py` 用 `StaticPool` 固定单连接 | 默认 QueuePool 下内存库每连接独立、跨 session 数据不可见；StaticPool 保证建表/写入/读取跨 session 一致 | 测试改 tmp_path 文件库 | M0 总工 |
| 2026-08-28 | A2 调度器进程化：M0 通用队列调度器（轮询领取→分派 Worker→回写 + 节流/熔断/断点自愈），业务执行经 `Worker` 抽象注入；`python -m foundation scheduler --loop` 独立进程 | 09 文档第三节「调度器进程化」+ 02 文档基线（sourcing/scheduler.py 账本/节流/熔断模式）通用化；职责边界清晰（M0 不实现选品源级账本/降频） | 线程内调度（uvicorn daemon，重启即丢，02 高风险项） | M0 总工 |
| 2026-08-28 | A2 节流/熔断状态为**进程内存态**（stage 级：连续失败 ≥2 熔断暂停 base×2^level，冷却自动恢复，成功清零），不持久化 | 与 09 文档「risk_control 探针恢复」语义一致；进程重启即重置（调度器重启自愈，不依赖历史熔断状态）；避免额外状态表 | 持久化熔断状态到 app_config/source_platform_states | M0 总工 |
| 2026-08-28 | CLI 默认 worker 用 `LoggingWorker`（仅留痕演示，不执行业务）；业务 worker 由各模块集成时注入 | 调度器 CLI 可独立演示/运维（--once/--loop）；避免 M0 依赖具体业务实现 | CLI 内嵌真实业务执行 | M0 总工 |
| 2026-08-28 | A3 风控口径对齐（预登记）：M0 基座实现通用预算三重硬约束/余额检测/一键全停（金额分 int、ROI 浮点、枚举英文），与 M5 `ads/stop_loss.py` S7/S8 同口径；共享规则以基座为准，M5 后续引用（改 M5 代码需总控协调） | 总控指示「共享规则以基座为准，M5 引用」，避免双实现口径漂移；已读 M5 stop_loss.py 确认 S7=check_budget_triple/S8=kill_switch_enabled 契约 | 完全由 M5 持有规则，M0 不实现 | M0 总工（口径裁决：总控） |
| 2026-08-28 | A3 风控规则引擎落地 `backend/foundation/risk.py`：通用四层防线（S7 预算三重/S1·S3 自动止损/S5 余额/S8 全停），与 M5 stop_loss.py **同签名同语义**（函数名/参数/RuleVerdict·BudgetVerdict·EngineResult/枚举/边界语义全对齐），M5 可直接 import 替换自有实现；S2/S4/S6（诊断优化记录/补贴统计/活跃上限）为投放业务专属留 M5 不清除 | 总控批准 A3「共享规则以基座为准，M5 引用」；同签名保证 M5 迁移零成本；纯函数/数据驱动可被全系统复用 | 复制 M5 实现到 M0（重复） | M0 总工 |
| 2026-08-28 | A4 通用脱敏基座 `backend/foundation/security.py`（redact_url/redact_text/redact_path）：对齐 M2 语义独立实现，**增强 Bearer <token> 掩码**（先 Bearer 后键值正则，避免 key= 规则吃掉 Bearer 前缀）；任何日志/证据写入前必须经本模块 | P-004 密钥绝不落日志；基座统一入口避免各模块重复实现；Bearer 是 Authorization 头常见形态需覆盖 | 各模块各自实现脱敏 | M0 总工 |
| 2026-08-28 | A4 默认库路径修正 `sqlite:///data/db/m0-foundation.db`（宪法第 4 节 backend/data/db/<模块>.db）+ `backend/.env.example` 全模块变量名模板（无明文值） | 对齐一模块一库路径规范；.env.example 供部署复制填写、防密钥散落（P-004） | 保持 data/ 顶层路径 | M0 总工 |
| 2026-08-29 | A4 修复（总控裁决）：`redact_text` 对 `Bearer <token>` **保留 Bearer 前缀、仅 token 脱敏**——`_REDACT_VALUE_RE` 替换回调 `_mask_secret_value` 遇值为 "Bearer"（token 已由 Bearer 规则掩码）时保留原文，输出形如 `Authorization: Bearer ***` | 总控裁决「Bearer 前缀保留，仅 token 脱敏」（实现/断言统一方向）；P-004 核心仍满足（token 无明文） | 放宽断言（掩盖实现不一致，被否） | M0 总工（裁决：总控） |
| 2026-08-29 | A5 迁移脚本用**纯 SQL 幂等脚本**（`database/migrations/0001_create_base_tables.pg.sql`：IF NOT EXISTS + ON CONFLICT DO NOTHING；回滚 0001_rollback.pg.sql 逆序 DROP），不引入 alembic 依赖；数据复制（SQLite→PG）建议 Python/SQLAlchemy 双引擎脚本（README 三节），与 M1 migrations 目录模式一致 | 零额外依赖、psql 可直接执行、幂等可重跑；alembic 留待生产环境按需引入；回滚方案以「切回 SQLite 快照」为基线（无数据损失） | alembic 迁移框架 | M0 总工 |
| 2026-08-29 | A6 六方会签销项（DA-008，M0/M1/M2/M3/M4/M5 + 总控裁决 REC-009~011）：数据字典最终口径=**安全/证据指纹 SHA-256 hex（商品/通用）、去重指纹 MD5 32 位 hex + phash（素材/文件判重）两类并存**；PAGE_CHANGED 保留供 M1/M4 使用（M2 映射 PLATFORM_REJECT 获批）；M1 app_config 键 `category.whitelist` 对齐（M1 S3c 后执行）；M4 独立维护 listing_tasks 不双写 M0 workflow_jobs（M0 workflow_jobs 作跨模块编排队列）；M5 已完成风控基座引用（import foundation.risk） | 会签六方确认口径一致；共享规则以基座为准落地（A3 预登记兑现）；指纹按用途分类避免素材 MD5 改造成本 | 全系统统一 SHA-256（M2 改造成本高且违背 05 文档） | M0 总工（裁决：总控 REC-009~011） |
| 2026-08-29 | A7 集成冒烟（test_foundation_integration.py 3 例）：M1→M0→M4→M5→回写闭环 mock/fixtures 跑通；**发现 M4 集成缺口**（pipeline 未落 SPU/SKU 本库 → 候选池 title/category/价格恒 None）登记 DA-009 提请总控转达 M4 修复，冒烟断言按缺口标注 | 闭环验证 M0 基座可编排全链（队列/调度/风控/脱敏）；跨模块问题不擅自改对方代码（宪法第 4/5 节），走 data-audit 上报 | M0 直接修 M4 代码（违规） | M0 总工 |
| 2026-08-29 | **M0 模块收官（v1.0）**：M4 DA-009 修复后收紧集成断言（候选池 title/category_id/price 非 None 且正确）→ `test_foundation_integration.py` 3 passed + foundation 82 passed 全绿；A1~A7 全部验收（基座/队列/调度/风控/脱敏/迁移/会签/集成闭环）；progress 100%、brief +实现快照 | A7 收官完成模块级验收；体系级全量回归与最终备份归总控（宪法第 12 节） | 继续等待外部依赖 | M0 总工 |
| 2026-08-29 | P2-4 备份协议增强：`backend/foundation/manifest.py` 通用 SHA-256 清单机制（`MANIFEST_FORMAT="m0-manifest-v1"`；`build_manifest` 确定性排序/重复路径报错/缺失即失败；`save/load/verify`；`ManifestVerification` 聚合校验——missing/mismatch 分别列出不中断；CLI `python -m foundation manifest build/verify`，verify 退出码 0=全通过；相对路径 posix 归一 + `base_dir` 参数 → 清单可移植换目录校验）+ 15 测试（foundation 子集 94 passed 零回归） | 对齐融合清单 P2-4「迁移包 SHA-256 清单机制（MANIFEST.json）｜数据可追溯」与旧系统 `build_material_manifest.py`（local_path/size_bytes/policy）+ `tools/migration_audit.py`（完整性审计）；关键交付物（DDL/迁移脚本/词表/语料）备份交接前后 build→verify 闭环；零新依赖（stdlib hashlib/json） | 目录递归/glob 展开（调用方显式传文件列表，防漏防误含）；仅 size 校验（无哈希，不可检测篡改） | M0 总工（新任） |
| 2026-08-29 | P2-2 业务语料归档：旧系统 `docs/migration/` 脱敏语料（`CHAT_TRANSCRIPT_SANITIZED.md` 1.42MB/3836 条 + `CHAT_HANDOFF.md` 6KB）→ `context/knowledge/corpus/` 只读存档 + README 登记（来源/用途=LLM 词表扩充/规则抽取/测试用例生成） | 融合清单 P2-2 指定去向 M0 知识库；语料已脱敏（文件头声明账号/手机号/密码/API 密钥/令牌已掩码，登记前抽查确认）；只读不改写 | 仅登记引用路径不复制（语料需随工作区可移植，故复制入知识库） | M0 总工（新任） |
| 2026-08-29 | P2-3 设计决策史归档：superpowers 27 篇（plans 15 + specs 12；融合清单口径「15 设计+11 计划」与实测 27 篇有出入，以文件系统为准）→ `context/knowledge/superpowers/` 三份决策要点归档（specs 12/12·103 条、plans 01-08 8/8、plans 09-15 7/7）+ 总索引 `knowledge/README.md`；3 子代理并行抽取，子代理 C 两次中断后由总工接管（P-014 进度累积：每文档立即落盘，中断可续） | 设计决策溯源（为什么这么设计），供新系统模块规划/评审对照；归档纪律：只读旧系统、要点自述不复制原文、无密钥、UTF-8 无 BOM | 子代理单会话长跑（中断风险，P-014 已按进度累积处理） | M0 总工（新任）+ 子代理 A/B |
