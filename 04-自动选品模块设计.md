# 04 自动选品模块设计

> 复用半成品 `sourcing_scheduler.py` / `sourcing_score.py` / `compliance.py`，新增「投放转化」维度，与上架→托管投放形成闭环。

> ✅ **已落地实现**：`backend/sourcing/`（Python 包，39 测试全绿）。离线 `fixtures` 模式零登录态跑通全链路；真实采集走 Playwright 共享 Chrome（选择器配置化）。用法见 `backend/README.md`。

## 一、选品来源（三源 + 数据补全）

| 来源 | 采集方式 | 数据 | 状态 |
|---|---|---|---|
| 抖店电商罗盘（正式第三源） | Playwright 共享浏览器（CDP 9223） | 商品榜/飙升榜（rank-product，Aurora 表格） | 基线已实测打通 |
| 有米云 | Playwright 独立浏览器（CDP 9555），商品榜扫描翻页 | 带货数据、素材热度 | 基线已实测打通 |
| 视频号商机中心 | Playwright 共享浏览器（CDP 9223） | 机会品（类目/价格带/排名） | 基线已实测打通 |
| 1688/淘宝 | 以图搜款 + 订单确认页询价 | 真实成本、材质、SKU、运费 | 半成品已验证 |

> 注（总控裁决 REC-006）：第三选品源以**抖店电商罗盘**为正式来源（基线已实测打通）；设计稿原「考古加（五个榜单：销量榜/视频热推/商品热销/数据大盘/往年爆款）」**降级为可选第四源**，待后续排期（Playwright 采集器从零开发）。

增量策略：静态榜扫完当天跳过；实时榜小时轮询，连续空转 24 次自动降日轮询。

## 二、选品流水线

```
采集（账本游标/节流/熔断）
  → 去重（image_phash + source_core_attributes_hash + 来源稳定性）
  → 合规三态（hard_reject / candidate / manual_review）
  → 数据补全（1688 逐 SKU 真实询价 + 淘宝同款素材）
  → 打分（五维，满分 100）
  → 排序取 Top N → 商品池
  →（可选人工复核闸门）→ 上架 → 托管投放
```

## 三、打分模型（v2，五维）

| 维度 | 满分 | 说明 | 数据来源 |
|---|---|---|---|
| 热度趋势 | 35 | 榜单排名 + 销量 + 多榜交叉确认加分 | 有米云/商机中心/抖店罗盘 |
| 利润率 | 30 | (售价-真实成本)/售价，成本取 1688 逐 SKU 询价最低有效方案 | 询价结果 |
| 售后风险 | 20 | 退货率分档：≤3% → 20，≤8% → 16，≤15% → 8，更高 → 0 | 平台数据 |
| 供给稳定 | 15 | 1688 供应商数 + 多榜同源数 | 竞品匹配 |
| **投放转化（新增）** | 权重可配（建议起步 10，从其他维折算） | 托管投放 ROI/成交额历史表现，按商品类目回写 | AdReportSnapshot |

> 偏好调整：正/负类目权重、价格带权重、素材特征权重、来源榜单权重、历史类目成本 —— 全部配置化（现 `SourcingPreference` 机制）。

## 四、合规过滤（复用 `compliance.py`，放宽配置）

- 三态：`hard_reject`（品牌侵权/禁售词/功效资质缺失）直接拒；`candidate` 进池；`manual_review` 人工确认
- **类目白名单改为配置项**：现 `ALLOWED_CATEGORIES` 仅家居/日用/宠物等 9 类，改造为可在后台增删的配置表，逐步放开口径
- 标题清洗：去品牌词/「同款」/「官方旗舰」/功效词（`sanitize_marketplace_sku_name`）

## 五、验收标准（对齐原项目口径）

- 日有效候选 ≥ 200（不是 200 次重复扫描）
- 去重率、合规拦截率、询价成功率、同款匹配率（≥80 分才算可靠同款）可观测
- 打分理由逐条可解释（`reasons` 字段）
- 上架后 14 天托管转化数据回流，验证「投放转化」维度有效性