# 11 里程碑与落地路线

## 一、前置条件检查清单

> 状态更新：2026-08-29（总控实测）｜ ✅=已就绪 ｜ ⏳=待用户确认/提供 ｜ ❌=未就绪（不阻塞 mock/fixtures 开发，阻塞真实模式）

| 类别 | 项目 | 状态 | 实测/说明 | 影响 |
|---|---|---|---|---|
| 资质 | 微信小店开通主体（企业/个体户）+ 类目资质 | ⏳ 待确认 | 需用户确认主体类型与已开通类目 | M4 live 上架、M5 实投 |
| 账户 | 小店账号、推广余额 | ⏳ 待确认 | 需用户提供/确认余额充足 | M5 v1.1 真实实投 |
| 账户 | 共享 Chrome 登录态（商机中心/抖店/1688/淘宝/有米云） | ✅ 已就绪 | CDP 9223/9555 实测在线，M1 真实采集 101 条已打通 | 采集/兜底/投放 UI |
| 账户 | 小店素材库登录态 | ⏳ 待确认 | 素材上传真实化前置（M3_UPLOAD_MODE=api/ui） | M2/M3 上传真实化 |
| 密钥 | DeepSeek API Key | ❌ 未设置 | `DEEPSEEK_API_KEY` 环境变量未配置；M3 文案/LLM 现走规则降级 | M3 真实 LLM 文案/复核 |
| 密钥 | Kimi / Wan API Key（可选） | ⏳ 待配置 | 生图真实化（现 Pillow 占位图） | M3 主图真实化 |
| 环境 | Python 3.12 | ✅ 兼容 | 实测 3.13.14，全量 1093 测试通过 | — |
| 环境 | Node 20+ | ✅ 已就绪 | 实测 v24.19.0 | 前端（如启用） |
| 环境 | Playwright Chromium | ✅ 已就绪 | import 正常 + CDP 在线 | 采集/兜底 |
| 环境 | ffmpeg / ffprobe | ✅ 已安装（9.0.1，winget） | 2026-08-29 安装；M2 标准化器/M3 视频二创真实模式自动启用（74 测试验证） | 真实转码已解锁 |
| 环境 | TikTokDownloader 4.1.x | ⏳ 待安装 | M2 封装就绪，依赖外部 CLI 安装 | M2 抖音/快手/小红书下载 |
| 数据 | 迁移包 `app.sanitized.db` → `backend/app.db`（498 products 基线） | ❌ 未找到 | 工作区无迁移包；现以 fixtures 为开发基线 | M4 上架候选历史基线 |
| 数据 | PostgreSQL 实例 | ⏳ 待准备 | M0 A5 迁移脚本已就绪（0001_create_base_tables.pg.sql） | M4 阶段生产存储 |
| 契约 | 官方 channels OpenAPI 文档核对（T1~T7：签名参数/接口字段/配额） | ⏳ 待核对 | M4 external-contracts.md 已列待核对项；mock 模式不依赖 | M4 live 模式 |
| GitHub | 仓库改 Private | ⏳ 待确认 | 当前 public；建议改 Private（Settings → Danger Zone） | 商业方案保密 |

> 说明：以上 ⏳/❌ 项全部不阻塞当前 mock/fixtures 全链路（体系已 1093 测试全绿）；逐项就绪后按「就绪即启用」策略自动切换到真实模式（对应模块的配置项/环境变量已预留）。

## 二、分期实施

### M1：基座 + 素材闭环（1–2 周）
- [ ] 迁移包在本机跑通：依赖安装、测试回归（534 passed）、共享浏览器登录
- [ ] 硬编码路径环境变量化（Chrome/便携浏览器/CDP）
- [ ] 素材库表 + 采集流水线（TikTokDownloader 封装 + 视频号采集器）
- [ ] 视频素材 ffmpeg 标准化（9:16 / ≥720×1280 / ≤500M / 5~300s）
- **验收**：素材库可入库、去重、预览；日采集量可观测

### M2：选品 + 上架闭环（2–3 周）
- [ ] 选品打分加「投放转化」维度（数据结构先行，无数据时权重=0 不生效）
- [ ] 类目白名单配置化
- [ ] `wechat_openapi.py` 薄封装 + 商品创建/审核状态轮询
- [ ] 上架校验硬门禁清单落地（标题/5主图/逐SKU定价/限购）
- [ ] Playwright 181KB 脚本降级为兜底通道
- **验收**：端到端模拟流程（选品→询价→生图→上架），真实链接才标「已上架」；不提交真实商品

### M3：素材优化 + 小店投放托管（2 周）
- [ ] 视频二创流水线（片头/字幕/多版出片）+ 文案管线
- [ ] 素材上传小店素材库自动化（拿素材 ID + 评估标签）
- [ ] `shop_ads` 模块：添加托管商品（≤50/批）→ 投放设置（ROI）→ 素材绑定
- [ ] 托管报表回读 + 止损规则引擎 + 余额检测
- **验收**：托管计划真实跑通（小额预算实投验证），报表快照/止损规则按时触发

### M4：数据回流与全自动迭代（持续）
- [ ] 订单/成交归因 → 选品打分「投放转化」维度生效
- [ ] 素材评估标签回流 → 素材优化模板参数重训练
- [ ] 人工闸门按数据达标逐步放松
- [ ] SQLite → PostgreSQL；调度器进程化
- **验收**：日有效候选 ≥200、托管 ROI 环比提升、素材「高效」占比上升

## 三、风险预案

| 风险 | 预案 |
|---|---|
| 平台 UI 改版（托管/上架页） | `page_changed` 检测 + 选择器证据留痕 + 人工接管；上架主线已走 API 不受影响 |
| 小店投放入口调整/无 API 化 | 保持 probe 脚本维护；功能降级为半自动（人工点击，系统预填） |
| 风控封禁 | 节流/熔断/探针恢复；登录态多套轮换 |
| 素材审核拒审率高 | compliance 预审 + 素材评估标签回流统计拒审原因 |
| 数据量增长 | M4 迁 PostgreSQL + MinIO + Redis |

## 四、角色与值守

- 日常值守：余额告警、验证码/登录接管、拒审处理（半自动）
- 周报：采集/选品/上架/托管四段漏斗（复用 `sourcing_report.py` 扩展）