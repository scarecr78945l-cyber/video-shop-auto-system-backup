# 08 自动小店投放模块设计（商品托管）

> 口径确认：投放 = 微信小店后台「小店投放」的**商品托管模式**（无转化不扣费、腾讯全域流量、平台自动投流），不走腾讯广告 Marketing API。以下字段与流程均以投放后台真实界面（2026-08 实测截图）为基准。

## 一、后台事实（自动化输入/输出锚点）

| 页面 | 关键事实 |
|---|---|
| 托管首页 | 已托管/托管中/待托管计数、可用余额、日预算（当前可不限）、「无转化不扣费」 |
| 添加托管商品 | 两步：① 添加商品（**上限 50/批**、自动过滤投放中商品、仅展示销售中商品、分桶：机会品/热搜品/优质商品/潜力商品）→ ② 投放设置 |
| 投放设置 | 目标三选一：**成交ROI / 净成交ROI（秒退不扣费）/ 商品成交**；目标 ROI（如 2.00，可取系统推荐）；**素材与视频号形象**绑定 |
| 投放管理列表 | 全部/投放中/暂停投放/不可投放；列：商品名+ID+分桶标签、目标出价（如成交ROI 2.40）、**智能诊断**（优秀/良好/1项待优化）、商品曝光数、花费、成交金额、**平台补贴**、操作（查看详情/添加素材） |
| 素材库 | 9:16 视频 ≥720×1280 / MOV·MP4 / ≤500M / 5~300s；评估标签 探索期/高效/潜力；审核不通过或源文件损坏的素材投放时不支持选择 |

## 二、全自动闭环

```
选品打分 Top N（上架销售中商品）──→ 自动进入托管队列（≤50/批）
        │
        ▼
素材库优选（高效 > 潜力 > 探索期）→ 绑定「素材与视频号形象」
        │
        ▼
自动执行托管两步：
  ① 添加商品（按选品排序勾选到上限）
  ② 投放设置：目标=成交ROI（默认）/净成交ROI（秒退单多时）；目标ROI=系统推荐值
     提交投放
        │
        ▼
监控回读（定时）：投放列表 曝光/花费/成交金额/平台补贴/诊断
        │
        ├─ 曝光>阈值 且 花费>0 且 0 成交 → 自动暂停 + 打标签「换素材/调ROI」
        ├─ 诊断「1项待优化」 → 记录优化项，优先换素材重投
        └─ ROI 达标 → 保持；补贴数据计入报表
        │
        ▼
数据回写：商品加「投放转化」维度进选品打分；素材评估标签回流素材优化
```

## 三、数据模型（字段对齐后台）

```python
class AdCampaign(Base):                  # 一个托管计划 = 1 商品 + 1 组投放设置
    id
    product_id
    ad_mode: str = "goods_trust"        # 商品托管
    target_type: str                    # 成交ROI / 净成交ROI / 商品成交
    target_roi: float                   # 如 2.00（默认取系统推荐）
    material_ids_json                   # 素材库 ID 列表（含视频号形象）
    status                              # 待托管/托管中/已暂停/不可投放/已结束
    diagnosis                           # 智能诊断（优秀/良好/1项待优化...）
    batch_id                            # 批量托管批次
    created_at, updated_at

class AdRun(Base):                       # 单次执行（复用 WorkflowJob 机制）
    campaign_id, attempt, status, error_code, evidence_json,
    lease_owner, lease_expires_at

class AdReportSnapshot(Base):            # 定时回读投放列表
    campaign_id, recorded_at,
    impressions, spend, gmv, platform_subsidy, diagnosis, status

class AdAccountState(Base):              # 投放账户状态（仿 SourcePlatformState）
    balance, status, throttle_level, paused_until, pause_reason

class AdMaterial(Base):                  # 素材库视频（与 Asset 打通）
    material_id, asset_id, file_path, duration, resolution,
    evaluation(探索期/高效/潜力), upload_status, platform_material_id
```

## 四、自动化执行细则

**① 添加托管商品**
- 数据源 1：选品打分 Top N 且状态=已上架（官方 API 可确认销售中）
- 数据源 2：平台自带分桶（机会品/热搜品/优质商品/潜力商品）作为加分输入
- 每次 ≤50 个，多批串行，批间隔可配（防风控）

**② 投放设置**
- 默认目标：成交ROI（多规格多价位商品适用）；秒退单多时切净成交ROI
- 目标ROI：优先系统推荐值；可配置覆盖（如统一 2.00）
- 素材：`高效 > 潜力 > 探索期` 优先级绑定；无素材则跳过该商品并标记「待素材」

**③ 提交与确认**
- 提交后进入「投放中」列表；页面校验反馈（余额不足/素材未过审）转 `blocked` + 人工接管（复用验证码接管模式，仅影响该任务）

## 五、监控止损规则引擎

| 规则 | 动作 |
|---|---|
| 花费>0 且 成交=0 且 曝光≥阈值（如 500） | 暂停该托管 + 标签「换素材/调ROI」 |
| 诊断=1项待优化 | 记录优化项到 evidence，标记优先重投 |
| 成交ROI < 目标×80%（持续 2 个快照周期） | 降素材优先级/建议调 roi |
| 平台补贴>0 | 计入报表，补贴后 ROI 单独统计 |
| 余额 < 阈值（如 ¥100） | 暂停新托管 + 告警（人工充值点） |
| 投放中商品数 > 上限（如 40/51） | 停止新增，等自然淘汰 |

## 六、数据回写（系统进化）

1. **选品打分第 5 维度「投放转化」**：按商品类目聚合 ROI/成交额，权重可配
2. **素材评估回流**：`evaluation` 标签由投放实际效果更新 → 素材优化模板参数按类目重训练
3. **托管失败/不可投放原因**（资质/素材/价格带）回写商品 `review_reason`，指导选品过滤规则